# Copy this file to .env.local
# For Vercel, add these in the project settings

# OpenRouter Configuration (PRIMARY)
# Replace with your real key. Do NOT commit your actual key.
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key-here

# Optional overrides
OPENROUTER_MODEL=gpt-oss-20b
OPENROUTER_API_BASE=https://openrouter.ai/api
PUBLIC_URL=http://localhost:8080
# Leave OPENROUTER_REFERER blank so the server uses your app's origin as HTTP-Referer
OPENROUTER_REFERER=

# Optional: Restrict models clients can request via options.model (comma-separated)
OPENROUTER_ALLOWED_MODELS=

# Optional: Tighten CORS in Vercel function to specific origins (comma-separated)
# e.g., http://localhost:8080,https://your-app.vercel.app
OPENROUTER_ALLOWED_ORIGINS=

# Optional: Local development proxy port
VITE_PROXY_PORT=5174

# Allow client to request a specific model (options.model) when enabled
# 1/true/yes to allow; blank/0 to disable
OPENROUTER_ALLOW_CLIENT_MODEL=1

# Optional: Production base URL for client fallback (e.g., your Vercel URL)
# VITE_PROD_URL=https://skippy-kohl.vercel.app

# Unlock API (server-side only)
# Set one or more passwords. Do NOT expose to client.
UNLOCK_PASSWORD=onestring7
# Or provide multiple separated by comma/semicolon/newline
# UNLOCK_PASSWORDS=onestring7,another-secret
UNLOCK_MAX_ATTEMPTS=5
UNLOCK_LOCKOUT_SECONDS=300
UNLOCK_SESSION_SECRET=change-me-long-random
UNLOCK_SESSION_TTL=86400
